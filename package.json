{
  "name": "portland-districting-poc",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "ArcGIS JS POC: contiguous district builder over the Portland area",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "gen:sample": "node scripts/generate-sample.mjs"
  },
  "dependencies": {
    "@arcgis/core": "^4.31.6"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
